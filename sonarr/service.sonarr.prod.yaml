apiVersion: v1
kind: Service
metadata:
  labels:
    app: sonarr
    environment: prod
  name: prod-sonarr
  namespace: servarr
spec:
  ports:
  - name: http
    port: 8989
    protocol: TCP
    targetPort: 8989
  selector:
    app: sonarr
  type: ClusterIP
