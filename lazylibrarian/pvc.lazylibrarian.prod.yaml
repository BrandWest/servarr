apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: lazylibrarian
    environment: prod
    type: local
  name: prod-lazylibrarian-config
  namespace: servarr
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      app: lazylibrarian
      environment: prod
      type: local
  storageClassName: ''
  volumeMode: Filesystem