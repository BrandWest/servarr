apiVersion: v1
kind: Service
metadata:
  labels:
    app: lidarr
    environment: prod
  name: prod-lidarr
  namespace: servarr
spec:
  ports:
  - name: http
    port: 9696
    protocol: TCP
    targetPort: 9696
  selector:
    app: lidarr
  type: ClusterIP
